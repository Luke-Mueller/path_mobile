class ListItem {
  constructor(item, items, name, itemType) {
    (this.item = item),
      (this.items = items),
      (this.name = name),
      (this.itemType = itemType);
  }
}

export default ListItem;
